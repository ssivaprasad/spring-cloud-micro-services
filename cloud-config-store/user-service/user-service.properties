app.status.check.message =  ${spring.application.name} is up and running : message from config-server : user-service.properties

server.port = 0

eureka.client.service-url.defaultZone=http://localhost:8765/eureka
## eureka.instance.instance-id=${spring.application.name}:${random.value}