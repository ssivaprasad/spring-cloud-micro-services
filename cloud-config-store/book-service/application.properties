app.status.check.message =  ${spring.application.name} is up and running : from config-server : book-service/application.properties

server.port = 0

eureka.client.service-url.defaultZone=http://localhost:8765/eureka
## eureka.instance.instance-id=${spring.application.name}:${random.value}